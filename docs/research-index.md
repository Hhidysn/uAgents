# 三方调研资料与归档记录

日期：2026-08-31。根目录：`F:\documents\software\uAgents`。

## 归档范围

原目录共 7 个顶层条目、3207 个文件、43,392,153 字节（约 41.4 MiB），包括一个独立的第三方 Git 工作区。移动前检查未发现重解析点或目录链接。

以下 6 个文件首次移入 `三方调研/` 时逐一验证了移动前后 SHA256 一致；用户随后将整个资料目录放入 `third-part-research/`，目前位于 `third-part-research/三方调研/`。保留原文，不把旧操作示例改写成新插件的授权规则：

| 文件 | 内容与使用限制 |
| --- | --- |
| `agents.md` | 原 CLI 清单及上一轮讨论时的修改；含已失效的全局路由引用，仅作历史参考 |
| `agents.md.before-uagents-20260831-010818.bak` | 上述清单的旧备份 |
| `个人订阅和agent配置方案.md` | 用户当时的订阅与分工想法；不是当前余额或价格证明 |
| `workbuddy-调用调研.md` | 内嵌 CLI 的调用记录；审批绕过示例不是默认授权 |
| `trae-调用调研.md` | 已区分三种同名 CLI，并实测 TRAECNclaw 经 CDP 调用 TRAE CN Solo、消耗桌面账号积分；正式实现不使用其中提取登录凭据或直调私有 API 的备选路线 |
| `豆包工作-调用调研.md` | 早期 CDP 页面发现、输入框写入与清除记录；后续真实闭环与正式实现另见 `docs/verification/2026-09-01-doubao-mcp.md` |

## 目录移动已完成

原位置：`F:\documents\software\uAgents\traecnclaw`

现位置：`F:\documents\software\uAgents\third-part-research\traecnclaw`

首次移动曾因本地 TRAECNclaw 网关占用目录而失败。当时监听 `127.0.0.1:8788` 的进程为 Node PID 34500，入口为 `scripts/start-gateway.js`，只读队列查询为零个未结束任务。该 PID 与任务状态仅是历史观察值。

自动停止并迁移的操作被环境拒绝，助手未执行停止或重启、未绕过限制。用户随后自行完成目录移动并添加 `/third-part-research/` 忽略规则；该规则已核验生效，助手保留用户的 `.gitignore` 修改。

2026-08-31 重新核对：归档仍有 3207 个文件、43,392,153 字节；嵌套 Git HEAD、两个已修改文件和已知未跟踪条目与迁移前一致。未对用户移动过程做完整逐文件哈希认证，不能将总数和大小一致描述为全部字节已验证。旧报告中的绝对路径保留为历史记录，不能照抄为安装配置；未自动改写全局配置。

## 第三方代码来源

- 上游：<https://github.com/Luckycat133/traecnclaw-mcp-skill>
- 本地 Git HEAD：`2d7fc66dfc86cf6a0967b87d0e28f5be087ae308`。
- 本地 `package.json` 声明版本：`0.6.0`；本地许可证文本：MIT，版权声明 Luckycat133 / 2026。
- 工作区不是干净的上游快照：`LICENSE`、`README.md` 有修改，`src/`、`scripts/` 等大量文件未跟踪。必须整体保留，不能重置、重新克隆覆盖或只保留 Git HEAD。
- 本地 README 的生成来源提交与 Git HEAD 不同；发布前必须重新核实实际采用的源代码、许可证与补丁来源。这里的版本号不是对官方发布包完整性的认证。

归档目录保持本机私有且被 Git 忽略。未来复用实现时，应把经审查的必要部分作为有来源和许可证记录的正式依赖或 vendor 组件引入，而不是从归档路径运行发行版。

## 整理阶段与后续评审的证据边界

- 已执行：路径与链接检查、文件移动与内容校验、Git 初始化、读取文档和源文件、只读查询已有网关状态。
- 未执行：外部模型请求、界面输入、任务提交、依赖下载、MCP 安装或注册、账号登录、凭据读取、全局 `AGENTS.md` 修改。
- 用户已完成源码移动；后续核对确认目录位置、文件总数/大小和 Git 状态与已知记录一致。
- 用户另行授权了多模型设计评审；其调用记录与结论保存在 `docs/reviews/`，不应与最初的“未调用外部模型”整理阶段混淆。
